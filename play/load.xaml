<Page
    x:Class="play.load"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:play"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <Button Content="下载"  Click="Button_Click_2"/>
            <Grid>
                
                <Image x:Name="img" VerticalAlignment="Center" HorizontalAlignment="Center"  Height="350"  Width="600" Source="Assets/gd.jpg" Opacity="1" />
                <MediaElement Name="MyMedia"
                            Margin="12"
                            VerticalAlignment="Center" HorizontalAlignment="Center"
                           AutoPlay="false"
                           Width="600"
                           Height="600"
			               AreTransportControlsEnabled="True">  
                </MediaElement>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
